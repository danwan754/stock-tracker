{
  "name": "stockTracker",
  "version": "1.0.0",
  "description": "This web-app will allow users to track their stocks. The UI is minimal and only show basic stock information. All market data are fetched from IEX.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "dependencies": {
    "express": "^4.16.3",
    "request": "^2.88.0"
  }
}
